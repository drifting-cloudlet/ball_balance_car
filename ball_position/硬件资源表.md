# 硬件资源表

## 芯片与开发环境
- **硬件平台**：MaixCAM2；现有代码另兼容 MaixCAM / MaixCAM-Pro
- **SoC 型号**：工程文件未记录
- **开发框架**：MaixPy Python API
- **运行入口**：`main.py`
- **模型输入**：MaixCAM2 为 640x160；MaixCAM / MaixCAM-Pro 为 320x320
- **最后更新**：2026-07-31

## 串口与接线

| MaixCAM2 端 | 对端 STM32F407 | 方向 | 配置 | 状态 | 冲突检测 | 依据 |
|-------------|---------------|------|------|------|---------|------|
| `U0T / UART0_TX` | `PC7 / USART6_RX` | MaixCAM2 发送 | `/dev/ttyS0`，115200，8N1，3.3 V TTL | 代码已启用，硬件未验证 | 未发现代码级冲突 | `CV_steer/maixcam2_steel_ball_uart/README.md` |
| `U0R / UART0_RX` | `PC6 / USART6_TX` | 预留反向通信 | 本次不读取 | 未使用 | 无代码占用 | 同上 |
| `GND` | `GND` | 共地 | 必须连接 | 硬件待确认 | 不适用 | 同上 |

## 软件管理资源

| 资源 | 分配方式 | 说明 |
|------|---------|------|
| UART pinmux | 系统设备节点管理 | 参考工程明确不在应用内配置 pinmux |
| UART 中断 / DMA | MaixPy 与系统驱动管理 | 应用代码不直接配置 |
| 摄像头 / 显示 / NPU | MaixPy 管理 | 保持现有 YOLO 工程配置不变 |

## 网表与约束检查
- 工程目录未检测到 `.net`、`.NET`、`.NetList`、`.kicad_net`、`.asc`、`.cir`、`.spice` 或网表命名文件。
- 本轮不新增引脚、不修改 pinmux，直接复用参考工程中已经定义的 UART0 设备节点和接线约定。
- SWD、STRAP、DMA 和中断优先级不由该 MaixPy 应用配置；板端接线与电平仍需实机复核。

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| 2026-07-31 | 初始生成 UART0 与平台资源基线 | RESEARCH 阶段对比本地参考工程 |
| 2026-07-31 | 标记 UART0 已接入应用，实机待验证 | EXECUTE / REVIEW 阶段完成主机侧实现 |
