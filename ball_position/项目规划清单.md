# 项目规划清单

## 项目信息
- **项目名称**：YOLO 钢球位置检测 UART0 功能对齐
- **硬件平台**：MaixCAM2（保留现有 MaixCAM / MaixCAM-Pro 兼容路径）
- **开发框架**：MaixPy Python API
- **当前阶段**：REVIEW
- **trace_id**：`yolo-uart-parity-20260731`
- **当前轮次**：Round 10/10
- **下次继续点**：在 MaixCAM2 上复测 -10、-5、0、+5、+10 cm 五个位置
- **停止条件**：新增第三方依赖、单轮改动超过 5 个文件、无法复用参考协议或主机回归失败 3 次时停止并回到 RESEARCH
- **创建日期**：2026-07-31
- **最后更新**：2026-07-31

## 轮次计划

| 轮次 | 目标 | 责任角色 | 验证标准 | 状态 | 备注 |
|------|------|---------|---------|------|------|
| 1 | 比较 CV 与 YOLO 工程，锁定功能差异和硬件约束 | Scout | 明确协议、调用点、清理路径和最小改动文件 | 已完成 | 未检测到网表；复用本地参考工程 |
| 2 | 实现 UART0 协议模块与主机回归 | Builder | 帧格式、量程、序号和写失败测试通过 | 已完成 | 7 项单元测试通过 |
| 3 | 移除 WebRTC 并接入 YOLO 逐帧 UART 发送 | Builder | 主程序无 WebRTC 引用；有球/丢球各发送一帧；退出关闭串口 | 已完成 | 仅 MaixCAM2 自动启用 |
| 4 | 更新打包说明并复核语法、测试与硬件边界 | Verifier | `unittest`、语法检查和关键字审查通过；资源表与代码一致 | 已完成 | 板端串口实测需用户完成 |
| 5 | 诊断 MaixVision 临时运行模块缺失 | Scout | 确认错误文件与缺失依赖链 | 已完成 | 日志运行的是旧版多文件入口 |
| 6 | 将板端入口改为单文件并回归 | Builder / Verifier | 无本地模块导入；模拟 Maix 导入与参考输出一致 | 已完成 | 模型仍使用板端绝对路径 |
| 7 | 修正 MaixCAM2 模型目录并保留旧目录兼容 | Builder / Verifier | 正式目录优先解析；语法与候选顺序检查通过 | 已完成 | 主机验证通过，板端待复测 |
| 8 | 反转横轴坐标符号并同步显示、UART 与论文图 | Builder / Verifier | 左端为 -12.5 cm，中点为 0 cm，右端为 +12.5 cm；UART 协议不变 | 已完成 | 映射断言、UART 7 项单测和 SVG 校验通过 |
| 9 | 将尺面红色 `0` 设为坐标零点 | Builder / Verifier | 轴比例 0.464 处为 0 cm；两端量程和 UART 协议不变 | 已完成 | 坐标 2 项与 UART 7 项回归全部通过 |
| 10 | 接入五点实际距离校正 | Builder / Verifier | 五组实测点准确映射；显示与 UART 同步；协议不变 | 已完成 | 坐标 3 项与 UART 7 项回归全部通过 |

## 功能模块清单

| 序号 | 模块名称 | 文件 | 状态 | 完成日期 | 备注 |
|------|---------|------|------|---------|------|
| 1 | YOLO 钢球检测与坐标换算 | `main.py`、`ball_position.py` | 已完成 | 2026-07-30 | 保持现有逻辑 |
| 2 | UART0 帧编码与发送 | `ball_uart.py` | 已完成 | 2026-07-31 | 对齐 CV 工程协议 |
| 3 | YOLO 主循环逐帧发送与状态显示 | `main.py` | 已完成 | 2026-07-31 | 有球发送有效帧，丢球发送无效帧 |
| 4 | UART0 主机回归测试 | `test_ball_uart.py` | 已完成 | 2026-07-31 | 7 项测试通过 |
| 5 | WebRTC 移除与应用打包说明 | `main.py`、`app.yaml`、`README.md` | 已完成 | 2026-07-31 | 不再运行或打包 WebRTC |
| 6 | MaixVision 单文件入口 | `main.py` | 已完成 | 2026-07-31 | UART、坐标投影和滤波已内联 |
| 7 | 横轴坐标方向反转 | `main.py` | 已完成 | 2026-07-31 | 显示与 UART 共用反向后的 `position_cm` |

## 开发日志

### 2026-07-31 - 完成差异研究并进入计划阶段
- 已确认 YOLO 工程已有坐标计算，但 UART 发送仅保留注释占位。
- 已确认 CV 工程使用 `/dev/ttyS0`、115200 和带 XOR 的 `$B` 协议。
- 下一步计划：先实现可独立测试的 UART 传输模块，再接入主循环。

### 2026-07-31 - 用户调整范围并进入执行阶段
- WebRTC 不再属于交付范围；将从主程序运行路径和应用打包清单中移除。
- 保留本机显示、图片采集以及默认关闭的 RTSP/JPEG 路径。
- UART 协议模块已完成 7 项主机回归，下一步接入 YOLO 主循环。

### 2026-07-31 - 完成主机侧实现与审查
- 已接入 MaixCAM2 UART0，有球/丢球每处理帧发送一次，并显示打开/写入错误。
- 已从 `main.py`、`app.yaml` 和 `README.md` 移除 WebRTC 运行与打包入口。
- 7 项单元测试和全部顶层 Python 语法检查通过；板端串口与 STM32 联调尚未执行。

### 2026-07-31 - 修复 MaixVision 临时运行模块缺失
- 板端日志显示 `/tmp/maixpy_run/main.py` 仍导入 `ball_uart`，临时目录没有该模块。
- 当前 `main.py` 已改为单文件入口，移除 `ball_uart` 和 `ball_position` 导入。
- 模拟 Maix 环境导入、UART 协议、有效/丢球/越界帧、坐标换算和滤波对比均通过。

### 2026-07-31 - 修正 MaixCAM2 模型目录
- 板端日志证明单文件入口已生效，但代码请求了旧目录名。
- 模型 ZIP 的 README 指定正式部署目录为 `/root/models/steel_ball_yolo11n_640x160_clean1077/`。
- 本轮将正式目录设为首选，并保留旧目录作为兼容候选；板端加载仍待复测。
