# 编辑清单

## 2026-07-31 - 接入五点实际距离校正
- **trace_id**：`yolo-uart-parity-20260731-cal5`
- **轮次/角色**：`Round 10 / Scout + Builder + Verifier`
- **修改文件**：`main.py`、`ball_position.py`、`test_ball_uart.py`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`；同步 `paper_figures/03_yolo11_pipeline.svg` 及静态预览
- **操作类型**：修改
- **操作内容**：按用户五组实测数据增加“当前读数→实际厘米”分段线性校正，校正结果同时用于屏幕和 UART
- **关联模块**：几何坐标换算、实际距离校正、屏幕显示、UART0 逐帧发送、论文流程图
- **验证标准**：五个标定点精确还原；内联与参考函数一致；UART 帧格式、丢球、越界和序号行为不变
- **验证结果**：通过；Python 语法和内联/参考函数一致性检查通过，坐标 3 项与 UART 7 项共 10 项单元测试全部通过
- **已知边界**：`±10 cm` 实测范围外采用线性外推，仍需 `±12.5 cm` 实测数据验证全量程边缘
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 将尺面红色 0 设为坐标零点
- **trace_id**：`yolo-uart-parity-20260731-zero464`
- **轮次/角色**：`Round 9 / Scout + Builder + Verifier`
- **修改文件**：`main.py`、`ball_position.py`、`test_ball_uart.py`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`
- **操作类型**：修改
- **操作内容**：新增 `AXIS_ZERO_RATIO=0.464`，按左端/零点/右端三点分段换算，使黄色零线和坐标零值对齐尺面红色 `0`
- **关联模块**：标定轴绘制、坐标换算、屏幕显示、UART0 逐帧发送
- **验证标准**：模型坐标 `x=297.32` 为 `0 cm`，左右端保持 `-12.5/+12.5 cm`；内联与参考函数一致；UART 协议不变
- **验证结果**：通过；Python 语法检查通过，内联/参考换算函数一致，坐标 2 项与 UART 7 项共 9 项单元测试全部通过
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 反转横轴坐标符号
- **trace_id**：`yolo-uart-parity-20260731-xinv`
- **轮次/角色**：`Round 8 / Builder + Verifier`
- **修改文件**：`main.py`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`；同步 `paper_figures/03_yolo11_pipeline.svg` 及静态预览
- **操作类型**：修改
- **操作内容**：交换横轴两端厘米标定，使图像左侧为负、右侧为正；屏幕显示与 UART 继续共用同一个 `position_cm`
- **关联模块**：坐标映射、屏幕显示、UART0 逐帧发送、论文流程图
- **验证标准**：左/中/右映射为 `-12.5/0/+12.5 cm`；Python 语法、UART 回归和 SVG 校验通过；协议及丢球行为不变
- **验证结果**：通过；坐标映射断言正确，UART 7 项单元测试全部通过，5 幅 SVG 的 XML、网格、DAG、静态碰撞及运行时检查全部通过
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 修正 MaixCAM2 模型目录
- **trace_id**：`yolo-uart-parity-20260731-fix2`
- **轮次/角色**：`Round 7 / Scout + Builder + Verifier`
- **修改文件**：`main.py`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`
- **操作类型**：修改
- **操作内容**：按模型 ZIP README 将 MaixCAM2 正式模型目录设为首选，保留旧目录兼容，并更新构建标识
- **关联模块**：MaixCAM2 启动与模型路径解析
- **验证标准**：语法检查通过；正式目录优先命中；正式目录缺失时旧目录可兼容命中
- **验证结果**：通过主机语法检查、UART 7 项单测及正式/旧目录路径解析测试；板端模型加载待复测
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 修复 MaixVision 单文件运行依赖缺失
- **trace_id**：`yolo-uart-parity-20260731-fix1`
- **轮次/角色**：`Round 5-6 / Scout + Builder + Verifier`
- **修改文件**：`main.py`、`app.yaml`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`
- **操作类型**：修改
- **操作内容**：把 UART、坐标投影和滤波实现内联到 `main.py`，移除本地模块导入；应用包改为只包含 `main.py` 和 `models`
- **关联模块**：MaixVision 临时运行入口
- **验证标准**：单文件语法通过；无 `ball_uart`/`ball_position` 导入；模拟 Maix 导入成功；内联实现与原模块输出一致
- **验证结果**：通过；首次模拟脚本因手写错误校验和值失败，改为与参考模块逐项比较后通过；板端重新运行待验证
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 完成 UART 接入、移除 WebRTC 并复核
- **trace_id**：`yolo-uart-parity-20260731`
- **轮次/角色**：`Round 3-4 / Builder + Verifier`
- **修改文件**：`main.py`、`app.yaml`、`README.md`、`项目规划清单.md`、`编辑清单.md`、`研究发现.md`、`硬件资源表.md`
- **操作类型**：修改
- **操作内容**：接入 MaixCAM2 UART0 逐帧有效/无效帧发送与状态显示；移除 WebRTC 运行和打包入口；更新说明与任务证据
- **关联模块**：YOLO 主循环、UART0、应用打包与文档
- **验证标准**：UART 单测、全部顶层 Python 语法、WebRTC 零引用、UART 调用点、打包文件存在性和硬件资源一致性检查
- **验证结果**：通过（7 项单测；Python 语法检查退出码 0；`main.py`/`app.yaml`/`README.md` 无 WebRTC 引用）；YAML 专用解析器不可用，未执行解析器验证；板端实机未验证
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 完成 UART0 协议模块并调整计划
- **trace_id**：`yolo-uart-parity-20260731`
- **轮次/角色**：`Round 2 / Builder`
- **修改文件**：`ball_uart.py`、`test_ball_uart.py`、`项目规划清单.md`、`研究发现.md`、`编辑清单.md`
- **操作类型**：新增 / 修改
- **操作内容**：新增独立 UART0 传输模块和 7 项主机回归；按用户要求把 WebRTC 移出交付范围
- **关联模块**：UART0 帧编码与任务规划
- **验证标准**：`python -B -m unittest -v test_ball_uart.py` 全部通过；协议模块语法检查通过
- **验证结果**：通过（7 项测试，退出码 0；语法检查退出码 0）
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无

## 2026-07-31 - 建立任务基线
- **trace_id**：`yolo-uart-parity-20260731`
- **轮次/角色**：`Round 1 / Scout`
- **修改文件**：`项目规划清单.md`、`编辑清单.md`、`硬件资源表.md`、`研究发现.md`
- **操作类型**：新增
- **操作内容**：记录 CV/YOLO 差异、轮次计划、硬件资源和研究证据
- **关联模块**：任务治理与 UART0 硬件基线
- **验证标准**：四份文件均位于 `YOLO_steer/webrtc_yolo` 且不改变应用运行文件
- **验证结果**：通过
- **Git提交**：无（目标工程不在 Git 仓库中）
- **远端同步**：未 push（无 Git 仓库）
- **回档记录**：无
